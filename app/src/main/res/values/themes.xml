<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodApp" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Primary branding color. -->
    <item name="colorPrimary">@color/white</item>
    <item name="colorPrimaryContainer">@color/white</item>
    <item name="colorOnPrimary">@color/white</item>

    <!-- Secondary branding color. (Progress bar color)-->
    <item name="colorSecondary">@color/navy</item>
    <item name="colorSecondaryContainer">@color/white</item>
    <item name="colorOnSecondary">@color/white</item>

    <!-- Background color. -->
    <item name="android:colorBackground">@color/white</item>

    <!-- Surface color. -->
    <item name="colorSurface">@color/white</item>
    <item name="colorOnSurface">@color/white</item>

    <!-- Error color. -->
    <item name="colorError">@color/white</item>
    <item name="colorOnError">@color/white</item>
</style>

    <style name="Theme.FoodApp" parent="Base.Theme.FoodApp" />

    <style name="CustomAlertDialog" parent="Theme.Material3.Light.Dialog.Alert">
        <item name="colorPrimary">@color/white</item>
        <item name="buttonBarPositiveButtonStyle">@style/CustomPositiveButton</item>
        <item name="buttonBarNegativeButtonStyle">@style/CustomNegativeButton</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <style name="CustomPositiveButton" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/navy</item>
        <item name="backgroundTint">@color/white</item>
    </style>

    <style name="CustomNegativeButton" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/navy</item>
        <item name="backgroundTint">@color/white</item>
    </style>
    
    
    <style name="App.Theme.FoodApp" parent="Base.Theme.FoodApp" />
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/navy</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_app</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.FoodApp</item>
    </style>
</resources>